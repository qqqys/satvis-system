# 卫星可视化系统前端

``` bash
// 把代码下载到本地
git clone git@github.com:qqqys/satvis-system.git   

// 进入目录
cd satvis-system   

// 安装项目依赖，等待安装完成之后，安装失败可用 cnpm 或 yarn
npm install         

// 开启服务器，浏览器访问 http://localhost:3000
npm run dev or yarn dev

// 执行构建命令，生成的dist文件夹放在服务器下即可访问
npm run build
```
